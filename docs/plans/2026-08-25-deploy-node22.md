# 部署工作流 Node.js 22 Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 让标签触发的 Vercel 部署命令运行在 Node.js 22 上。

**Architecture:** 只调整现有 GitHub Actions `setup-node` 输入，不改变触发器或部署命令。通过静态检查确认版本与变更范围。

**Tech Stack:** GitHub Actions、Node.js 22、Vercel CLI

---

### Task 1: 更新部署 Node.js 版本

**Files:**
- Modify: `.github/workflows/deploy.yml:34`

1. 将 `node-version: 20` 改为 `node-version: 22`。
2. 运行 `pnpm exec biome check .github/workflows/deploy.yml`，预期退出码为 0。
3. 运行 `git diff --check` 并确认没有其他配置变化。
4. 提交并推送 `main`；不创建 `v*` 标签。
